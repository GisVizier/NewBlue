--[[
    Paths in player data that are "peekable" (visible to other players).
    Keys are dot-separated paths into the data tree.
]]

return {
	["Data.Currency"] = true,
	["Data.CharacterData.Race"] = true,
}
